<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 9+ (API 28, the app's targetSdk) blocks plain-HTTP ("cleartext")
  traffic by default. The Momthathel backend is served over plain HTTP on
  http://10.0.2.2:8980, so cleartext must be explicitly permitted for the
  local dev hosts below. 10.0.2.2 is the emulator's alias for the host
  loopback. HTTPS endpoints are unaffected and still use the system trust.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
