# ── Build outputs ───────────────────────────────────────────────────────
**/target/
**/bin/
**/build/
**/.dart_tool/
**/.flutter-plugins
**/.flutter-plugins-dependencies

# ── IDE / editor cruft ──────────────────────────────────────────────────
.project
.classpath
.settings/
*.iml
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# ── Logs / temp ─────────────────────────────────────────────────────────
*.log
*.tmp
*.bak

# ── Local env / secrets (KEEP for the future — current OMSConstant secrets
#    are in source on purpose per this branch's push instruction) ───────
.env
.env.*
*.local

# ── One-off DB bootstrap script (created during hot-patch session) ──────
Backend/liquibase-bootstrap.sql
